@{
    var user = await UserManager.GetUserAsync(User);
}

@if (user != null)
{
    <section class="account-profile">
        <div class="profile">
            <div class="profile-image">
                <img src="/images/uploads/profiles/@user.ProfileImg" alt="@user.FirstName @user.LastName" />
                <form enctype="multipart/form-data" method="post" asp-action="UploadProfileImage" asp-controller="Account">
                    <input id="imageUploader" type="file" name="file" hidden />
                    <label for="imageUploader" class="btn btn-circle btn-circle-sm"><i class="fa-solid fa-arrows-rotate"></i> </label>
                </form> 
           </div>

          <div class="profile-info">
              <h3 class="displayname">@user.FirstName @user.LastName</h3>
              <p>@user.Email</p>
          </div>
            <button class="btn-theme-s"><i class="fa-solid fa-gear"></i> Account details</button>

            <button class="btn-login">Sign out<i class="fa-solid fa-arrow-left-to-bracket"></i></button>
           

       </div>
        <script src="~/js/site.js" asp-append-version="true"></script>

    </section>
}